---
title: Hello, world
date: 2026-08-25
description: A placeholder post showing the canonical frontmatter. Delete me.
tags:
  - meta
draft: true
---

This draft exists so the first real post has a template to copy. It builds in
local dev but is excluded from production, so it will never appear on the live
site.

The frontmatter above is the complete v1 Post model: `title`, `date`, and
`description` are required, `tags` needs at least one entry, and `draft` and
`updated` are optional.
